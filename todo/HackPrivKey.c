#include <stdio.h> 

int
main(int argc, char** argv)
{
        // take n, guess p and q with factorization
        // set(n/i) for i in xrange(1,n) if n%i == 0
        // returns set(n, p, q)
        ;

        // use keygen with p q to get private key
        ;
}