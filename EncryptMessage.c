#include <stdio.h>

int
main(int argc, char** argv)
{
        // Have someone encrypt message (m) using the public key (n,e)
        // c(m) = m^e mod n 
        ;

        // Decrypt the message C using your private key (n, d)
        // m(c) = c^d mod n
        ;

        return 0;
}